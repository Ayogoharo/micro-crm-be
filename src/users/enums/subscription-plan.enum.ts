/**
 * Subscription plan tiers available to users.
 */
export enum SubscriptionPlan {
  /** Free tier with basic features */
  FREE = 'free',
  /** Pro tier with advanced features */
  PRO = 'pro',
}
