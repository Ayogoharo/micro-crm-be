# Test Environment Configuration
# This file is used for E2E tests

# Application
NODE_ENV=test
PORT=4001

# Frontend (for CORS configuration)
FRONTEND_HOST=localhost
FRONTEND_PORT=3000

# Database - SEPARATE TEST DATABASE
# Use a dedicated test database to avoid polluting development data
DATABASE_HOST=localhost
DATABASE_PORT=5434
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=micro_crm_test

# JWT
JWT_SECRET=test-secret-key-for-e2e-tests
JWT_EXPIRES_IN=7d

# CORS (automatically built from FRONTEND_HOST and FRONTEND_PORT)
# The CORS origin will be: http://${FRONTEND_HOST}:${FRONTEND_PORT}
